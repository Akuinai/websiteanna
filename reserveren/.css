body {
    padding: 0px;
    margin: 0px;
    min-height: 100%;
    background: rgb(0, 0, 0);
    overflow: hidden;
}
.container {
    width: 100%;
    position: relative;
    border: none;
    display: flex;
    justify-content: center;
    align-content: center;
    padding: 0px;
    margin: 0px;
    flex-flow: row;
}
.user {
    width: 100%;
    height: 40px;
    border-radius: 5px;
    background: #000000;
    color: #ffffff;
    line-height: 40px;
    margin: 0px;
    box-sizing: border-box;
    padding: 5px;
    position: relative;
}
.success {
    width: 100%;
    text-align: center;
    position: relative;
    padding: 6px;
    margin: 8px auto;
    background: #000000;
    color: white;
    box-sizing: border-box;
    border-radius: 5px;
}

.error {
    padding: 6px;
    color: white;
    text-align: center;
    position: relative;
    width: 100%;
    box-sizing: border-box;
    margin: 8px auto;
    background: rgba(57, 56, 56, 0.67);
    border-radius: 5px;
}
.container .inserts {
    width: 50%;
    background: rgb(48, 47, 47);
    border: none;
    text-align: center;
    padding: 20px;
    min-height: 100vh;
    height: auto;
    box-sizing: border-box;
}

.container .views {
    width: 50%;
    background: rgb(0, 0, 0);
    border: none;
    text-align: center;
    padding: 20px 10px;
    min-height: 100vh;
    height: auto;
}
.container .views .links {
    width: 100%;
    display: flex;
    background: rgba(0, 0, 0, 0.6);
    box-sizing: border-box;
    height: 500px;
    padding: 0px;
    overflow-x: hidden;
}
.container .views .links .body {
    width: 100%;
    background: white;
    margin: 3px;
    border: none;

    border-radius: 5px;
    height: 400px;
}

.body h4 {
    width: 100%;
    padding: 4px;
    box-sizing: border-box;
    margin: 0px;
    background: #4c4949;
    color: white;
    font-size: 1.5vw;
}
.container .views .links a {
    display: block;
    width: 100%;
    text-decoration: none;
    color: rgba(0, 0, 0, 0.53);
    font-size: 1.5em;
    text-align: center;
    margin: 20% auto;
    padding: 5px;
    box-sizing: border-box;
}
.view_more {
    padding: 5px;
    background: rgb(0, 0, 0);
    box-sizing: border-box;
    border-radius: 20px;
    position: sticky;
    bottom: 0px;
    font-size: 17px;
    color: white;
}
h2 {
    color: rgba(0, 0, 0, 0.75);
}
h1 {
    color: white;
    padding: 5px;
}
.container .inserts form {
    position: relative;
    width: 90%;
    margin: 8% auto;
    padding: 20px;
    border: 1px solid #fafafa;
    border-radius: 20px;
}
.inserts form input {
    display: block;
    border-radius: 10px;
    padding: 10px;
    width: 100%;
    border: none;
    outline: 0;
    box-sizing: border-box;
    font-family: Georgia, "Times New Roman", Times, serif;
}
.inserts form input:focus,
.inserts form textarea:focus {
    box-shadow: 0px 1px 10px #000000;
    transition: all 0.5s;
}
.inserts form input[type="submit"] {
    color: white;
    cursor: pointer;
    font-size: 20px;
    background: rgba(0, 0, 0, 0.42);
}
.inserts form textarea {
    font-family: Georgia, "Times New Roman", Times, serif;
    display: block;
    border-radius: 10px;
    padding: 10px;
    width: 100%;
    resize: none;
    border: none;
    outline: 0;
    box-sizing: border-box;
}